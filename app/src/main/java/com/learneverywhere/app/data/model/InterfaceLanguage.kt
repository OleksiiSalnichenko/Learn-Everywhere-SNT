package com.learneverywhere.app.data.model

/**
 * Мова інтерфейсу застосунку (історія 36 / R37). Не плутати з мовою
 * словника — це лише те, якою мовою показуються підписи й тексти UI.
 */
enum class InterfaceLanguage {
    UKRAINIAN,
    ENGLISH,
    GERMAN,
}
