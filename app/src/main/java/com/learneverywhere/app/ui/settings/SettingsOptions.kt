package com.learneverywhere.app.ui.settings

/**
 * Дозволені значення для числових налаштувань програвання (повтори, паузи) —
 * критерій приймання тікета 04: "обираються зі списку варіантів 1-6 секунд,
 * не довільним вводом" (брифінг п.3.3.9). Один спільний список для всіх
 * семи числових полів `AppSettings` — і пауз, і кількостей повторень.
 */
object SettingsOptions {
    val SECONDS_OPTIONS: List<Int> = (1..6).toList()
}
